{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests available\"",
    "lint": "eslint .",
    "build": "echo \"No build steps necessary\"",
    "deploy": "vercel",
    "logs": "vercel logs",
    "inspect": "vercel inspect",
    "clean": "rm -rf node_modules && rm -rf package-lock.json && npm cache clear --force && npm install"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^5.1.0",
    "mongoose": "^7.0.3",
    "nodemon": "^2.0.22",
    "validator": "^13.9.0"
  }
}